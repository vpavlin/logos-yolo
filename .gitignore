# Build + deploy artifacts
result
result-*
build/
build-*/
*.so
*.dylib
target/
*.tar.gz
*.log

# Qt / CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
*.moc
moc_*.cpp
qrc_*.cpp

# Scaffold dogfood scratch (regenerated by `basecamp setup`)
.scaffold/
scaffold.toml

# Runtime data (identity, cache) — never version-controlled
sequencer.key
sequencer.checkpoint
sequencer.checkpoint*
subscriptions.json
channel.id
cache/
media_cache/
storage/

# Local basecamp dev scratch
modules/
plugins/
.claude/
